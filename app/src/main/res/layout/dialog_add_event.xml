<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="24dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/glass_card_stroke"
    app:cardBackgroundColor="@color/glass_dialog_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ajouter un événement"
            android:textSize="20sp"
            android:textColor="@color/glass_text_primary"
            android:textStyle="bold"
            android:layout_marginBottom="16dp"
            android:layout_gravity="center_horizontal"/>

        <EditText
            android:id="@+id/et_event_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Titre de l'événement"
            android:textSize="16sp"
            android:textColor="@color/glass_text_primary"
            android:textColorHint="@color/glass_text_hint"
            android:padding="16dp"
            android:background="@drawable/bg_glass_input"
            android:layout_marginBottom="16dp" />

        <EditText
            android:id="@+id/et_event_description"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:hint="Description"
            android:textSize="14sp"
            android:textColor="@color/glass_text_primary"
            android:textColorHint="@color/glass_text_hint"
            android:padding="16dp"
            android:background="@drawable/bg_glass_input"
            android:gravity="top"
            android:layout_marginBottom="16dp" />

        <EditText
            android:id="@+id/et_event_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Heure (HH:MM)"
            android:textSize="16sp"
            android:textColor="@color/glass_text_primary"
            android:textColorHint="@color/glass_text_hint"
            android:padding="16dp"
            android:background="@drawable/bg_glass_input"
            android:inputType="number" />
            
        <!-- Note: Assuming Buttons were handled via DialogFragment builder or not present in original. 
             If original had no buttons (handled by AlertDialog), I leave it as is. 
             But usually custom dialog views might need buttons if not using standard AlertDialog buttons.
             The original file did NOT have buttons, so I assume they are added by AlertDialog builder. -->

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>