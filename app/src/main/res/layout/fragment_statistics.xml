<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color"
    tools:context=".statistics.StatisticsFragment">

    <!-- Top Bar with Horizontal Scrollable Days -->
    <LinearLayout
        android:id="@+id/horizontal_scrolable_days"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="@color/surface_color"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Placeholder for Calendar Icon/Button -->
        <ImageView
            android:id="@+id/btn_calendar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_statistics" 
            android:padding="8dp"
            app:tint="@color/primary_color"
            android:contentDescription="Calendar" />

        <!-- Placeholder for Horizontal RecyclerView of days -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_weeks"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:reverseLayout="true"
            tools:listitem="@layout/item_week" />

    </LinearLayout>

    <!-- Main Content Container -->
    <FrameLayout
        android:id="@+id/content_of_the_day"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/horizontal_scrolable_days"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        
        <!-- Content will be injected here (Empty or Record Available) -->
        
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
