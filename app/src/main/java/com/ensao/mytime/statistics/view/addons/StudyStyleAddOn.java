package com.ensao.mytime.statistics.view.addons;

import android.view.View;
import com.ensao.mytime.statistics.view.StatsViewGenerator;

public class StudyStyleAddOn extends StatsViewGenerator {

    public StudyStyleAddOn(View view) {
        // Apply study-specific styling if needed
        // For example, finding all CardViews and setting elevation dynamically
        // Currently following the same minimal pattern as WakeStyleAddOn
    }
}
